# G1 progress suite reports

No-teleport autofly scores for G1-after-dual-Q plan (`--replay-manual`, **yaw 90 north**).

Baseline flight: `bin/logs/perf_20260913-210134_42156.jsonl`

Per phase: `g1_<phase>_*.json` (flight_sim report) + `g1_<phase>_score.json` (phase gates).

**Product gate of record** is west `--scenario product-174657` — see
`bin/suite_reports/g1_a10_relight/`. North P5 (`230002`/`230452`) is progress smoke only.
