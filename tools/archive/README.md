# Archived tools

Legacy scripts kept for reference. Not used in the current resource-pack pipeline.

| Script | Notes |
|--------|-------|
| `import_blocks.ps1` | Pre–resource-pack block import (TD-008) |

Use `tools/rebuild_release_resource_packs.py` and `tools/migrate_to_resource_pack.ps1` instead.
