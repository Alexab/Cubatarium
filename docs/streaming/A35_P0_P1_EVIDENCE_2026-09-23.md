# A35 P0 / P1 evidence pack (realign)

Date: 2026-09-23  
SHA: A35 realign commit (`fix: A35 realign — SourceMismatch storm…`); resolve with `git rev-parse --short HEAD` on a clean tree.  
Constraint: `dirty_diff_hash=clean` + `exe_hash` required to close Gate 1.

## Manual hang / white class samples (pre-fix)

| Session | Class | Notes |
|---|---|---|
| `bin/logs/perf_20260923-171410_17444.jsonl` | GeometryMissing / publish starve | MeshWarmup ~75s dirty≈293; `pub_reject_source_mismatch≈49819`; opaque 18–21/86; no shutdown |
| `bin/logs/perf_20260923-170746_27720.jsonl` | GeometryMissing → opaque_on→0 | Enter then empty; `chunk_not_ready≈86`; SourceMismatch 30765 |

Primary category (A31 P1 table): **geometry not published** under SourceMismatch remesh thrash — not LightInvalid (0 after A34), not legal dark.

## Code attribution (A35 R0)

- CPU Apply compared `sourceRevision` to `InputStamps[0].content` → unbounded SourceMismatch → MarkDirty loop.
- Cross compared `PublishRevs.light_rev` to `MeshedLightRevision` observational lag.
- EnterLit `mesh_dirty=0` while MeshService dirty≫0 → soft settle false-clear.

## Gate status

| Gate | Status |
|---|---|
| Clean SHA AF visible | OPEN until post-commit suite |
| Far checkpoints | harness exists; UNTESTED this SHA |
| Operator eye | UNTESTED |
| holes=0 whole-route | FAIL / OPEN |
| Ring | OFF |

## Visible cold AF (A35 P0 attempt)

| Field | Value |
|---|---|
| Report | `bin/suite_reports/a35_realign/cold.json` |
| Perf | `bin/logs/perf_20260923-180933_24568.jsonl` |
| hang_killed | false |
| empty_world_stop_line | **PASS** (`opaque_cmd_on_med=58`) |
| enter_dirty_residual_stop_line | **PASS** (timeout residual stamps 0) |
| dual_lane / eye_proxy / A24 holes | FAIL (Gate 8 still OPEN — expected) |
| operator_visual | UNTESTED |
| dirty_diff_hash | non-empty (untracked tree noise) — Gate 1 not closed |

Primary class samples above still stand for hang autopsy; AF confirms empty-world + enter-dirty bindings after A35.
