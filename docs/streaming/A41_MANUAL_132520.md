# A41 manual 132520 — west eye after VisualObligation SoT

Date: 2026-09-24  
Perf: `bin/logs/perf_20260924-132520_44704.jsonl`  
Binary: post-A41 Release (pre–SLA-remint). Score: `bin/suite_reports/a41/manual_132520_score.json`.

## Route

`cx 7→−3`, `cz=3` (product corridor). 25 periods. Kick=0.

## vs baselines

| Metric | 132520 | 103228 (A40 live) | 102349 (plateau) |
|---|---|---|---|
| near_focus_holes gt0 | 8/25 (med 0, last 1) | 32/44 | 0/22 |
| chunk_meshed_unlit med/last | 28 / 26 | 17 / 17 | 35.5 / 37 |
| pending_light med/last | 31 / 62 | 80 / 80 | 53.5 / 69 |
| unfinished last | 90 | 82 | 91 |
| VB last | 70 | 164 | 73 |
| legal_dark | ~0 | ~0 | ~0 |
| demand_stop last | 0 | 0 | 0 |

Stop state matches **102349-class plateau**: PL stuck, unlit>15, holes=1, `draw_oracle_stale_vertex_light_n=70`, `mark_relit_schedule_n=0`, `mesh_dirty_schedule_skip_snapshot_n≈350`, `dirty_dropped=3540`, SoftDeferHeld=0.

## Root cause (code)

1. LightRepair stamped `visual_attempt_id` once; `visual_deadline_ms=3000` was **duration write-only** (never compared to clock).
2. Remint gated on `attempt_id==0` **and** `!ColumnHasRemeshOwner(dirty|…)` — Dirty stuck in skip_snapshot counted as owner → **forever silence**.
3. Admit skipped MarkDirty while Dirty present → Relight-only enqueue; at stop `relight_fifo_n=0`.
4. NotePending wiped `attempt_id` on every Note* (even dup suppress).

## Fix landed (follow-up)

- `ShouldRemintLightRepairDirty` / absolute SLA deadline (`now+3000`).
- Live pipeline = RAA|GPU|inflight (**not** Dirty-only).
- MarkRelit + Admit remint Invalidate+Dirty on SLA; open_sky still_stale also SLA Dirty.
- NotePending keeps live LightRepair attempt.

## Gate 8

| Check | Status |
|---|---|
| operator_visual | FAIL / plateau (pre-fix eye) |
| dual unlit≤15 | FAIL (med 28) |
| holes periods_gt0→0 | FAIL (8/25) |
| PL drain / demand_stop | FAIL at stop |
| Kick | PASS (0) |
| merge_green | false |

Re-eye after SLA rebuild; then scale=1 AF matrix.
