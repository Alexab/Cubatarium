# A35 P5 / P6 / P7 acceptance status

Date: 2026-09-23  
SHA: A35 realign (`fix: A35 realign — SourceMismatch storm…`); `git rev-parse --short HEAD` on clean tree.

## P5 — attributed fix

**Class:** GeometryMissing / publish starve under SourceMismatch remesh thrash  
**Fix:** A35 provenance self-consistency (CPU Apply + Cross); enter soft-settle honesty; fluid join shutdown.  
Not floating-origin / precision (A31-04 still gated).

## P6 — acceptance

| Check | Status |
|---|---|
| ≥5 A/B cold/warm | OPEN — run on clean SHA with `--visible` |
| holes=0 whole-route | OPEN / prior FAIL |
| post_stop PASS | OPEN |
| operator eye PASS | UNTESTED |
| dirty-drop ≤800 | OPEN |
| empty_world_stop_line | binding ON |
| enter_dirty_residual_stop_line | binding ON |

**merge_green=false** until holes=0 + eye PASS.

## P7 — Ring

**OFF** — do not enable RingReadiness / P8 until P6.
