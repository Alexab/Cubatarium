# A21 P8 profile-gated second-order options

Only after corrected baseline profile. Each requires before/after on same AF workload + unchanged correctness gates.

| If dominates | Option | Constraints |
|---|---|---|
| relight-remesh | separate light field from geometry | memory/halo/visual equiv |
| upload/retirement stalls | persistent mapping / ring uploads | GLES fallback required |
| transparent ordering | hierarchical resort region | WBOIT not drop-in for water |
| steady-state cost | LOD/HLOD subsystem | boundary contracts; not stale-visibility substitute |

Drop shadow adapters after cutover. KEEP diagnostic fixtures.
